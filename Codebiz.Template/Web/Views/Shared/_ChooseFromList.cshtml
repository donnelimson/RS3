@model Dictionary<string, object>

    <div class="modal-header">
        <h4 class="modal-title" id="modal-title" style="color:#67809f">List ({{module}})</h4>
    </div>

    <div class="modal-body" id="modal-body">
        <div class="body" ng-form="cForm">
            <div class="row" style="padding-bottom:10px">
                <div class="col-md-12">
                    <div class="form-group">
                        <label class="control-label">Search</label>
                        <input type="text" name="Name" class="form-control" autocomplete="off" maxlength="50"
                               ng-model="f.Searcher" ng-keydown="search(SearchText,$event)" ng-change="navigate()">
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <table ng-table="tableParams" class="table table-bordered">
                        <tr ng-repeat="d in $data" @*ng-dblclick="onSelectDoubleClick(d,$index)"*@ ng-click="onSelectClick($event,d,$index)" style="cursor:pointer" ng-class="{'selected':d.selectedRow!=null}">
                            @if (Model["ObjectType"].ToString() == "ITM")
                            {
                                <td data-title="'CODE'" header-class="'table-header text-left'" class="text-left">{{d.ItemCode}}</td>
                                <td data-title="'LONG DESCRIPTION'" header-class="'table-header text-left'" class="text-left">{{d.LongDescription}}</td>
                                <td data-title="'ACTIVE'" header-class="'table-header text-center'" class="text-center">
                                    <span class="label label-sm label-info ng-binding" ng-show="d.IsActive=='YES'">{{d.IsActive}}</span>
                                    <span class="label label-sm label-danger ng-binding" ng-show="d.IsActive=='NO'">{{d.IsActive}}</span>
                                </td>
                            }
                        </tr>
                        <tr ng-show="$data.length == 0">
                            <td colspan="14" class="text-center" style="font-style:unset">
                                No data available
                            </td>
                        </tr>
                    </table>
                    <span ng-show="offSetCount!=0"> Showing {{currentPage * tableCount-9}} to {{(currentPage * tableCount)-(tableCount-offSetCount)}} of {{totalRecordCount | number}} entries</span>
                    <span ng-show="offSetCount==0"> No result found</span>
                </div>
            </div>

        </div>
    </div>
    <div class="modal-footer pull-left">
        <!--Save-->
        <button type="button" class="btn btn-success" style="width: 90px;" ng-click="select()">
            <span class="glyphicon glyphicon-ok"> </span>&nbsp; Select
        </button>
        <!--Cancel-->
        <button type="button" class="btn btn-default" style="width: 90px;" ng-click="close()">
            <span class="glyphicon glyphicon-warning-sign"></span>&nbsp; Close
        </button>
    </div>
    <div class="clearfix"></div>

